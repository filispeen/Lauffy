# Bot-only profile for a separately hosted NodeLink v3 instance (Lavalink API v4).
services:
  discord-bot:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - lauffy-data:/app/data

volumes:
  lauffy-data:
